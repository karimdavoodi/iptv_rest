#include <iostream>
class Hardware {
    public:
        static std::string input_tuners();
        static std::string output_tuners();
        static std::string scan_input_tuner(const std::string&);
};
